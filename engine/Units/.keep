; вообще-то тут должны лежать модели

сейчас - роутер вызывает метод в контроллере

метод в контроллере - вызывает создает класс модели и вызывает в ней метод обработки

полученные данные оформляются для вьюхи

в нашей парадигме Units - это именно классы моделей

поэтому контроллеры надо положить куда-то в другую папку

Router ->
    (Entrypoint:
        <-> (Unit
                <- DB),
        <- Template,
    )
